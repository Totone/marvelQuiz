import React from 'react';

const Header = () => {
  return (
    <header>
      <section className="banner-container">
        <h1>Web Quiz</h1>
      </section>
    </header>
  );
};

export default React.memo(Header);
